<%= bootstrap_form_for(@ticket) do |f| %>
  <%= f.text_field :title, id: :ticket_title, label: 'Titel*', required: true, maxlength: 40 %>
  <%= f.text_area :message, id: :ticket_message, style: "min-height: 100px", label: 'Beskrivning av händelsen*', required: true, maxlength: 2000 %>
  <%= f.text_field :dateOfEvent, id: :ticket_dateOfEvent, class: 'datepicker', label: 'Datum för händelsen*', required: true, readonly: true %>
  <%= f.collection_select(:user_ids, User.all, :id, :name, {label: "Skicka till*", 
                          selected: @ticket.users.map(&:id)}, class: "select2", multiple: true, required: true, readonly: true) %>
  <%= f.primary "Skicka"%>
<% end %>

<p>
  <strong>OBS</strong> När du klickat på skicka kommer du tas tillbaka 
  till startsidan och ett meddelande kommer synas med ett 12-siffrigt id.
  Spara det numret om du vill kunna få återkoppling på ditt ärende.
</p>
